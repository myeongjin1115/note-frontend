import React from "react";
import styled from "styled-components";
import { Link } from "react-router-dom";

const DoneFollowing = styled.div``;

export default ({ username, following, posts, likes }) => (
  <DoneFollowing></DoneFollowing>
  // createdAt
);
